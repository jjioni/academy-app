# 구글 드라이브 + 스프레드시트 연동 설정

회원가입 시 첨부하는 신분증/통장사본 사진은 구글 드라이브에 자동 저장되고, 수강생 등록·출석·결제 기록은 지정한 구글 스프레드시트에 자동으로 한 줄씩 쌓입니다. 이 문서대로 한 번만 설정하면 됩니다. 새 구글 계정을 만드는 게 아니라, 지금 쓰시는 드라이브/시트에 접근 권한(서비스 계정)을 하나 초대하는 방식입니다.

## 1단계. 서비스 계정 만들기 (무료, 5분)

1. https://console.cloud.google.com 접속 → 로그인
2. 상단에서 새 프로젝트 생성 (이름 예: academy-app)
3. 좌측 메뉴 `API 및 서비스` → `사용 설정된 API 및 서비스` → `+ API 및 서비스 사용 설정`
   - `Google Drive API` 검색 → 사용
   - `Google Sheets API` 검색 → 사용
4. 좌측 메뉴 `API 및 서비스` → `사용자 인증 정보` → `+ 사용자 인증 정보 만들기` → `서비스 계정`
   - 이름 아무거나 입력 (예: academy-app-bot) → 만들기 및 계속하기 → 완료
5. 생성된 서비스 계정 클릭 → `키` 탭 → `키 추가` → `새 키 만들기` → JSON 선택 → 다운로드
   - 다운로드된 JSON 파일 안에 `client_email`과 `private_key` 값이 있습니다. 이 두 값을 3단계에서 사용합니다.

## 2단계. 기존 드라이브 폴더 · 스프레드시트에 권한 공유

1. 다운로드한 JSON 파일에서 `client_email` 값 복사 (예: `academy-app-bot@academy-app.iam.gserviceaccount.com`)
2. **신분증/통장사본 저장용 드라이브 폴더** 하나 새로 만들기 → 오른쪽 클릭 `공유` → 방금 복사한 이메일 주소 추가 → 권한 `편집자` → 공유
   - 폴더를 열었을 때 주소창의 `folders/` 뒤에 있는 긴 문자열이 **폴더 ID** 입니다. 3단계에서 사용합니다.
3. 지금 쓰시는 **스프레드시트**("해피니언 디자이너 양성과정..." 등) 우측 상단 `공유` → 같은 서비스 계정 이메일 추가 → 권한 `편집자` → 공유
   - 스프레드시트 주소창의 `/d/` 와 `/edit` 사이 긴 문자열이 **스프레드시트 ID** 입니다.

## 3단계. Render에 환경변수 등록

Render 대시보드 → 해당 서비스 → `Environment` 탭 → 아래 4개 추가:

| Key | Value |
|---|---|
| `GOOGLE_CLIENT_EMAIL` | JSON 파일의 `client_email` 값 |
| `GOOGLE_PRIVATE_KEY` | JSON 파일의 `private_key` 값 (`-----BEGIN PRIVATE KEY-----`부터 `-----END PRIVATE KEY-----`까지 통째로 붙여넣기) |
| `GOOGLE_DRIVE_FOLDER_ID` | 2단계에서 확인한 폴더 ID |
| `GOOGLE_SHEET_ID` | 2단계에서 확인한 스프레드시트 ID |

저장하면 자동으로 재배포되며, 이후부터 회원가입 서류는 드라이브에, 수강생 등록·출석·결제 기록은 스프레드시트의 `앱_수강생`/`앱_강사`/`앱_출석`/`앱_결제` 탭에 자동으로 쌓입니다(탭이 없으면 첫 기록 때 자동 생성됩니다).

## 참고

- 이 4개 값 중 하나라도 비어있으면 사진 업로드·시트 동기화는 조용히 건너뛰고 나머지 앱 기능은 평소대로 동작합니다(회원가입 자체가 막히지는 않습니다).
- 시트에 반영되는 데이터는 앱 → 시트 방향의 단방향 기록입니다. 시트에서 직접 수정해도 앱에는 반영되지 않습니다.
- `GOOGLE_PRIVATE_KEY`를 붙여넣을 때 줄바꿈이 깨져도 괜찮습니다 (`\n` 이스케이프 형태로 들어와도 코드에서 자동 처리합니다).
